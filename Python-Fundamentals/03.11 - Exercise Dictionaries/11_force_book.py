# dictionary = {}
#
# command = input()
# while command != 'Lumpawaroo':
#     if "|" in command:
#         force_side, force_user = command.split(" | ")
#         if force_side not in dictionary:
#             dictionary[force_side] = []
#         if force_side not in dictionary[force_side]:
#
#     if "->" in command:
#         force_side, force_user = command.split(" -> ")
#
#     command = input()
